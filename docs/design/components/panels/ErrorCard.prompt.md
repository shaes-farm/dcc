Degraded-state card shown inside a panel when its provider is unreachable. Detail must be actionable.

```jsx
<ErrorCard
  detail="401 from Grafana — check GRAFANA_TOKEN in your shell"
  action={<Button size="sm">Test connection</Button>}
/>
```
