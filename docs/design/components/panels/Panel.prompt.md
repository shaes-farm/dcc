The universal DCC container: bg-1 fill, hairline border, 34px header (title · "as of Xs" · actions). Set `pad={false}` for edge-to-edge tables/logs.

```jsx
<Panel
  title="Pods (qa)"
  asOf="8s ago"
  actions={<IconButton label="Refresh">⟳</IconButton>}
>
  …
</Panel>
```
