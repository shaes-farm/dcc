Persistent 28px bottom bar — workspace name, active layout preset, polling state, "system OK / N issues" beacon, 24h clock.

```jsx
<StatusBar
  workspace="Acme Commerce"
  preset="On-call"
  issues={5}
  clock="02:14"
/>
```
