Icon-only button for panel headers/toolbars (refresh, maximize, drawer close).

```jsx
<IconButton label="Refresh panel">⟳</IconButton>
<IconButton label="Pause polling" active>⏸</IconButton>
```

Always pass `label`. 28×28px hit area; ghost by default, `active` shows accent tint.
