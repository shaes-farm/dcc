Text input. Use `mono` whenever the value is an identifier, URL, query, or env-var name.

```jsx
<Input placeholder="Filter services…" />
<Input mono placeholder="https://qa.acme.dev/checkout/openapi.json" invalid />
```
