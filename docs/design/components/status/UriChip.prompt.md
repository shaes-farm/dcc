Chip rendering a stable resource URI (`service://checkout`, `doc://repo-md/…`). Use wherever an object reference is shown — Context panel rows, lineage nodes, correlation threads.

```jsx
<UriChip uri="workload://qa/checkout/deployment/checkout" onClick={jump} />
```
