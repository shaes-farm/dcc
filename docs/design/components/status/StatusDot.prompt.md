Status dot rendering the normalized vocabulary: healthy | degraded | failing | deploying | unknown. Never invent other statuses; never use color without a nearby label/glyph.

```jsx
<StatusDot status="failing" /> <StatusDot status="deploying" pulse />
```
