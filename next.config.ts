import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No config yet. Note that host binding, port, and telemetry are handled by
  // the `dcc` CLI (bin/dcc.mjs), not here.
};

export default nextConfig;
